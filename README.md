# TS_Adam
The code in this project demonstrates the performance differences between TS_Adam and other optimizers such as Adam in long-term and short-term predictions.


# Usage
1. Install Python 3.12 For convenience. execute the following command.

 ```bash
   pip install -r requirements.txt
```

2. Prepare Data. You can obtain the well pre-processed datasets from [Catbox](https://files.catbox.moe/hexlog.zip). Then place the downloaded data in the folder `./dataset`
  
4. Use bash command, such as "bash ./scripts/long_term_forecast/ETT_script/MICN_ETT_run.sh" to run the example with set parameters
